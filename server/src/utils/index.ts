/**
 * Shared utility placeholder.
 *
 * Utils hold small, pure, stateless helper functions with no knowledge of
 * Express, Prisma, or business rules (e.g. formatters, string/number helpers,
 * pagination math). They are the one layer every other layer may import from.
 *
 * Anything that touches the database, HTTP request/response, or encodes a
 * business rule belongs in `services/` or `middleware/` instead.
 *
 * Example:
 *   export function formatBytes(bytes: number): string { ... }
 */

export {};
